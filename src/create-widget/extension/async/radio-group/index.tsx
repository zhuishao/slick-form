import RadioGroup from '../../../widgets/antd/radio-group';
import AsyncComponent from '../index';

const AsyncRadioGroup: any = AsyncComponent(RadioGroup);

AsyncRadioGroup.displayName = 'AsyncRadioGroup';

export default AsyncRadioGroup;
