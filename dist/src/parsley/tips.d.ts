import React from 'react';
import { TipsInterface } from './Interface/Interface';
declare function Tips({ steps, step, switchSteps, ending, element, okText, jumpText, previousText, width: boxWidth, theme, }: TipsInterface): React.JSX.Element;
export default Tips;
