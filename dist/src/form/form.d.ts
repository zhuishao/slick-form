import React from 'react';
import './index.less';
import { FormLibProps } from './type.form';
declare const _default: ({ fields, widgets, readOnly, disabled, disabledFields, form, column, gridStyle, className, initialValues, onValuesChange, locale, getScrollContainer, scrollToFirstError, readOnlyEmptyValueNode, formConfig, readOnlyClean, forceUpdate, name, ...rest }: FormLibProps) => React.JSX.Element;
export default _default;
