import React from 'react';
import './index.less';
declare const _default: ({ form, name, event, widgets, fields, disabled, readOnly, operation, operationText, maxCount, leastOne, label, actionRef, grid, }: any) => React.JSX.Element;
export default _default;
