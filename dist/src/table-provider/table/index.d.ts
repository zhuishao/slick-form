import React from 'react';
import { TableProps } from '../types';
import './index.less';
declare const _default: ({ title, rowKey, columns, tools, rowOperations, filterIds, reload, pagination, size, emptyNode, rowSelection, tableId, drag, onDragDone, dragColumn, autoNo, keepRowSelectionHistory, style, virtual, loadMoreData, loadMoreTopData, ...restProp }: TableProps) => React.JSX.Element;
export default _default;
