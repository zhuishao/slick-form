import React from 'react';
import './index.less';
import AlertProps from './type';
declare const NoticeBar: ({ message, marqueeProps, marquee, ...props }: AlertProps) => React.JSX.Element;
export default NoticeBar;
